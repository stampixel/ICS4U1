static int abs(int x)
// Retruns the absolute valuie of integer x

static double abs(double x)
Returns the absolute value of real number x

static double pow(double base, double exp)
// Retrusn base*exp. Assumes base > 0, or base = 0 and exp > 0, or base < 0 and exp is an integer

static double sqrt(double x)
Returns sqr(x), x >= 0

static double random()
// Returns a random number r, where 0.0 <= r <= 1.0